apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    # Adjust to your real Prometheus instance's URL.
    url: http://prometheus:9090
    isDefault: true
    editable: false
